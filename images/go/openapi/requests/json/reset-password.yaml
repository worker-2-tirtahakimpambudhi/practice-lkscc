description: "Request body when reset password user"
content:
  "application/json":
    schema:
      type: object
      required:
        - password
        - confirm_password
      properties:
        password:
          type: string
          minLength: 8
        confirm_password:
          type: string
          minLength: 8